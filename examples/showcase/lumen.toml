[package]
name = "incident-workspace-showcase"
version = "0.3.0"
description = "Multi-file workspace example for imports, tools, effects, pipeline, machine, and smoke checks"

[dependencies]
# Standalone showcase workspace.
