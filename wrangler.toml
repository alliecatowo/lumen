name = "lumen-registry"
main = "workers/registry/worker.ts"
compatibility_date = "2024-01-01"
workers_dev = true

# R2 Bucket for package storage
[[r2_buckets]]
binding = "REGISTRY_BUCKET"
bucket_name = "lumen-registry"

[vars]
REGISTRY_PUBLIC_URL = "https://lumen-registry.alliecatowo.workers.dev/api"

[build]
command = "echo 'No build required for TypeScript worker'"
